<%= form_with(model: @equipment, local: true) do |eForm| %>
  <%= eForm.label :name %>
  <%= eForm.text_field :name %>

  <%= eForm.label :affiliate_link %>
  <%= eForm.text_field :affiliate_link %>

  <%= eForm.submit "Submit" %>
<% end %>
