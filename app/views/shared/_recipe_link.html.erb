<!--
if current_user.admin?
<%= link_to "Edit", edit_recipe_path(rec) %> | <%= link_to "Delete", rec, method: :delete, data: {confirm: "Delete this?"} %> | 
-->
<%= link_to rec.convert_name_for_title, "/recipes/#{rec.name}" %>
